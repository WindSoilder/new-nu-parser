[package]
name = "new-nu-parser"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tracy-client = { version = "0.17.3", default-features = false } # for tracy v0.11.1
logos = "0.15"
nu-cmd-lang = { version = "0.101", default-features = false, features = ["os"] }
nu-command = { version = "0.101", default-features = false, features = ["os"] }
nu-engine = { version = "0.101", default-features = false, features = ["os"] }
nu-protocol = { version = "0.101", default-features = false, features = ["os"] }
reedline = { version = "0.51.0", default-features = false }

[profile.profiling]
inherits = "release"
debug = true

[features]
# By default, profiling is disabled. Enable it by the "profile" feature
tracy = [
    "tracy-client/enable",
    "tracy-client/flush-on-exit",
    "tracy-client/sampling",
    "tracy-client/code-transfer",
]

[lib]
name = "new_nu_parser"
path = "src/lib.rs"

[dev-dependencies]
insta = { version = "1.33.0", features = ["glob"] }
tango-bench = "0.6"

[[bench]]
name = "benchmarks"
harness = false
